---
phase: M1
title: 总线+接管最小闭环
status: active
started: 2026-09-19
---

# M1 · 总线+接管最小闭环（进行中）

验收标准（两台真机+录屏）：lead 发起→成员同意→lead 实际敲进成员会话看到回显；拒绝/超时/断开三路径各测一遍。

## 任务序（钉死）
0l. ✅ **实机认证准备**：dsh 宿主禁绑 0.0.0.0（安全栅栏，尊重不绕）→ 用户态 lan-forwarder（0.0.0.0:3182→127.0.0.1:3180，无需管理员）；局域网真实路径自测全通（192.168.1.17 经转发器 ping/fleet 200；成员 WS 走 局域网IP+转发器+upgrade 上线 LANReal）。成员机一键包：scripts/member-boot.cmd + MEMBER-SETUP.md（三步：clone→npm i→改三行启动）；验收手册 docs/ACCEPTANCE-M1.md（#0-#10 清单+预期+录屏要求）。待办：第二台物理机照手册跑一遍+录屏（用户配合）。
0k. ✅ **M2 v2：IM 收方向 + 进度摘要流**：/im/callback（?t= 团队令牌鉴权 401 实测；决定词解析 允许/只读/拒绝 + 可选 C-id，无 id 作用于最新 remote pending；答复经总线回传 lead）——手机回「允许」通道即建（E2E m2-2）、回「只读」ro 通道+写 403（m2-4）；审批卡文本带 id 与答复格式。进度摘要：成员机 summarize()（干活中/等待输入/疑似卡住>10min/空闲 + 一行摘录）事件驱动即时+30s 周期上报，lead 设备卡实时显示（m2-3）。通用 IM 桥协议：POST {text,from} → {ok,understood,acted}。
0i. ✅ **M1 收口：门控统一**：/remote/write 挂同意门（active+rw+target 匹配 member:X，三态 403：no-consent/readonly-channel/target-mismatch 实测）；成员端指令执行双验证（本机须有 active 远端 rw 通道才执行 pty-* 指令，防 lead 侧被攻破后任意写）。
0j. ✅ **M2 v1：IM 出站桥 + SOS**：imBridge（im.json {enabled,webhook}，飞书自定义机器人格式/通用 JSON 自适应）——成员机收到 lead 连接请求自动推审批卡（含跳转成员页链接）、SOS 同步推 IM；本地接收器实测真发真收；lead 页设备卡 SOS 徽标+toast；成员页「呼叫 lead 协助」按钮（总线 sos 消息+审计）。E2E: m2-1-sos.png。
0g. ✅ **跨机总线 v1（M1 主体）**：成员出站 WS 连 lead（registerUpgrade+ws 库）；协议=hello/presence/sess-event/pty-out/consent-decided ↑，consent-request/pty-open/pty-write/pty-kill ↓；fleet 路由（成员在线/会话投影/事件/pty 尾流）；/pairing 配对（leadUrl+团队令牌+角色；TERMFLEET_* 环境变量覆盖供单机双实例）；远端通道=请求(member:X)→成员机真弹卡→decide 经总线回传→lead active→startPty/sendCmd 走 /remote/write→成员 pty 执行→输出流回传 lead 页实时显示。**修三个真 bug**：重连风暴（连上仍 5s 重建僵尸连接→conns 反复清空，改为 close 才重连+旧连接 close 不删新注册）、requestRemote 短路（共享文件已有同 id 时补 remote 标记）、sendCmd 远端分支漏换（fetch 未发）。双实例 E2E 全通：z-3-FINAL.png（lead 页 2 行真实回显+成员机执行实证）。
0h. ⏳ 真两台机验收（录屏）——单机双实例已全通，待真实第二台机。
0e. ✅ **决策笔记库 v2=write-notes-like-deepseek 治理完整落地**（2026-09-19 用户纠偏：不是薄 md）：目录即状态（proposed/implemented/rejected/archived 四态树）+ 六分类（feature/bug-fix/simplification/architecture/process/testing）+ 合法流转拦截（proposed→implemented|rejected；implemented|rejected→archived；非法 409）+ 校验门（/memory/verify：title/正文/分类/proposed 须含备选段）+ 决策看板统计行 + 面板状态/分类双筛选 + 流转菜单 + 详情含状态与可流转 + 蒸馏默认 implemented/process 带「备选」模板段 + 旧扁平自动迁移 + 真实项目决策种子 6 条；流转即目录移动（git 可见）。E2E：迁移/筛选/流转/非法拦截/校验全合格/蒸馏默认值，notes-*.png。
0f. ✅ src 镜像重建机制：src/index.ts=lib 逐字镜像+TS 头（scripts/rebuild-src.cjs 再生），杜绝手拼漂移（教训：mirror 脚本把代码插进头注释区，损坏被提交固化）。
0d. ✅ **同意总线 v0 + 实时会话流落地**（2026-09-19，主干）：consentStore（握手卡状态机 pending/active(rw|ro)/denied/ended/expired + 30min 限时 + 落盘 consent.json + 全操作入审计）+ 5 路由 + SSE 实时流（/stream?k=pty|events，宿主 listener→页面，dsh 事件 seq 级实时到达实测 8 条）+ **PTY 服务端同意门控**（无通道 403 / 只读禁写 403 readonly-channel，浏览器与 API 双验证）；远程页=真通道（请求→成员侧同意卡→倒计时→实时终端→发指令带 actor 真回显→断开）。面板升级=v7 原型整体移植（居中 1180px 六 tab 详情窗回归原型形态）。九步 API gauntlet + 浏览器 E2E 全过（v8-*.png）。
0c. ✅ **面板对齐原型完整 IA**（2026-09-19 用户纠偏：真面板不能丢掉原型那套框架）：四页导航（任务板/避坑库/远程/审计）+ 任务详情六 tab（文档大纲/全流程真时间线/会话·工作流·diff 待总线占位/决策笔记真关联避坑库）+ 审计真事件流（auditStore 埋点全操作）+ 远程页本机 PTY 真探针。
0b. ✅ **避坑库提前**（2026-09-19 用户排期纠偏：记忆无总线依赖不该等 M3）：memoryStore 文件式团队记忆（~/.dsh/termfleet/team-memory/*.md）+ 面板避坑库视图 + 任务完成自动蒸馏钩子。hippo federation 可指向该目录成共享层（M0 已验）。
0. ✅ **任务面板真实现 v1**（2026-09-19，提前于总线）：host 半任务库（~/.dsh/termfleet/tasks.json 落盘）+ 4 路由（app 页/列表/创建/action）+ 单文件面板页 /dsh-termfleet/app（生成器 scripts/gen-app-html.mjs，官方图标 21 枚内联，看板/列表/新建/认领/流转/删除/详情抽屉真实 history）——浏览器全交互实测（real-*.png），全程过鉴权门。教训：中文身份头必须 encodeURIComponent（Windows 管道脏字节会让 fetch 静默抛异常）。
1. ✅ 前置：doclib 文档库建立（2026-09-19）
2. ✅ 鉴权门：全部路由 Bearer/?token= 校验（2026-09-19 四 curl 实测过，timingSafeEqual+fail-closed）
3. ✅ 设计基线侦察：21 原子+令牌速查+挂载配方落档 docs/design-baseline.md（2026-09-19）
4. ⏳ 低保真三版已出（docs/mockups/m1-mockups.html，A 表格/B 卡片工作区★推荐/C 看板，亮暗双主题）→ **等用户圈点定稿**
5. 配对（邀请码→设备令牌）+ 握手卡状态机
6. lead WS 端点 + 成员出站 WS + 会话/PTY 中继
7. 限时 + 审计日志
8. 两台真机验收 + 录屏 + 笔记登记 + 文档同步（doc:sync-module）
